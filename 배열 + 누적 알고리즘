#include <stdio.h>

int main() 
{
  int n, score[10];
  int sum = 0;
  scanf("%d", &n);
  for (int i=1; i<=n; i++)
    {
      printf("%d번의 학생 점수는? :", i);
      scanf("%d", &score[i-1]);
    }
  for (int i=1; i<=n; i++)
    {
      sum = sum + score[i-1];
    }
  printf("%d", sum);
  return 0;
}
